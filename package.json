{
  "scripts": {
    "start": "sudo anchor build && sudo anchor deploy && sudo anchor test",
    "local-blockchain": "sudo solana-test-validator"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.18.0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3"
  }
}
